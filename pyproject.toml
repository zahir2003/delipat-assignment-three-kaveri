[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "delipat-assignment-three-kaveri"
version = "0.1.0"
description = "Delipat Assignment Three implementation for Kaveri Infrasystems"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
